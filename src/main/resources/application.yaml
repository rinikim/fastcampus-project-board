debug: false
management.endpoints.web.exposure.include: "*"  # actuator all feature

logging:
  level:
    com.fastcampus.projectboard: debug
    org.springframework.web.servlet: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace # query log -> ? binding check

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/board
    username: uno
    password: thisisTESTpw!#%&
    driver-class-name: com.mysql.cj.jdbc.Driver # mysql 8 version -> + cj
  jpa:
    open-in-view: false
    defer-datasource-initialization: true # make test jpa data (resource in query)
    hibernate.ddl-auto: create
    show-sql: true
    properties:
      hibernate.format_sql: true
      hibernate.default_batch_fetch_size: 100 # bulk query (id in)
  sql.init.mode: always

---

spring:
  config.activate.on-profile: testdb
#  datasource:
#    url: jdbc:h2:mem:board;mode=mysql
#    driver-class-name: com.mysql.cj.jdbc.Driver
#  jpa.hibernate.ddl-auto: create
#  sql.init.mode: always